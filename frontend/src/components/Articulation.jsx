// TEMPORARY — owned by Tanisha. Delete when her version lands. Do not merge this over hers.

export default function Articulation({ phoneme, level }) {
  return (
    <div className="aspect-video grid place-items-center bg-paper rounded-[10px] border border-dashed border-line">
      <span className="mono text-muted text-sm">animation slot · {phoneme}</span>
    </div>
  );
}
